@model List<BookViewModel>
@{
    ViewData["Title"] = "Book List";
}

<div class="text-center">
    @foreach (BookViewModel book in Model)
    {
        <a  asp-controller="Books" asp-action="@book.Isbn">        
            <h1 class="display-4">@book.Title</h1>
        </a>
       
        @if (string.IsNullOrEmpty(book.CoverPhotoUrl))
        {
            <p>No Cover Image Available @book.Title</p>
        }
        else
        {
            <img src="@book.CoverPhotoUrl" alt="Book Cover Image" width="100">
        }
        @if (string.IsNullOrEmpty(book.Genre))
        {
            <p>No Genre Available</p>
        }
        else
        {
            <p>@book.Genre</p>
        }
        @if (book.YearPublished.HasValue)
        {
            <p>@book.YearPublished</p>
        }
        else
        {
            <p>No Year of Publication available.</p>
        }

        @if (book.Copies == null)
        {
            <p>No Copies Stocked by This Library.</p>

        }
        else
        {           
            <p>Total Library Copies @book.Copies.Count</p>
        }
    }
</div>